ALTER TABLE TrainingProgram
ADD COLUMN prerequisite_id INT NULL,
ADD COLUMN cert_id INT NULL;

ALTER TABLE TrainingProgram
ADD CONSTRAINT fk_prerequisite
    FOREIGN KEY (prerequisite_id)
    REFERENCES TrainingProgram(training_id)
    ON DELETE SET NULL;

ALTER TABLE TrainingProgram
ADD CONSTRAINT fk_training_cert
    FOREIGN KEY (cert_id)
    REFERENCES Certification(cert_id)
    ON DELETE SET NULL;
